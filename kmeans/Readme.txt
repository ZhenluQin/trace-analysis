# kmeans_model realizes the function about the unsupervised learning
# Input: the extrace_information that doesn't have the proper label, 
         just have the features
# Output: the clstuer of the given k parts